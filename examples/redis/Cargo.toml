[package]
name = "redis_test"
version = "0.0.1"
authors = ["Hossam Maher<hos.mercury@gmail.com>"]
edition = "2021"

[dependencies]
axum = {version = "0.7"}
tokio = { version = "1", features = ["full", "tracing"] }
async-trait = "0.1"
axum_session = { path = "../../", features = [ "redis-db"] }
redis_pool = "0.2"
redis = { version = "0.23" }